포트 번호의 의미:

컴퓨터가 인터넷에 연결될 때, IP 주소는 **"건물 주소"**와 같아요. 하지만 한 건물에 여러 상점이나 부서가 있을 수 있듯이, 한 컴퓨터 안에서도 여러 프로그램(웹 서버, 이메일 프로그램, 게임 등)이 동시에 인터넷을 사용하고 있을 수 있어요.

포트 번호는 이러한 프로그램들을 구분하는 "방 번호" 또는 "상점 번호" 같은 역할을 합니다. 특정 프로그램이 네트워크를 통해 데이터를 주고받을 통로를 지정해 주는 거죠.

예를 들어, 웹 서버는 보통 80번 포트(HTTP)나 443번 포트(HTTPS)를 사용하고, Flask 예시에서는 8080번 포트를 사용했어요. http://127.0.0.1:8080에서 :8080이 바로 포트 번호입니다.

기본 충돌 시 해결 방안:

만약 두 개 이상의 프로그램이 **동일한 IP 주소와 동일한 포트 번호를 사용하려고 하면 "포트 충돌"**이 발생해요. 한 방에 두 사람이 동시에 들어가려고 하는 것과 같아서, 하나는 실패하게 됩니다.

Flask 서버를 실행하려는데 "Address already in use" 또는 "Port already in use"와 같은 오류 메시지가 뜬다면 포트 충돌이 발생한 거예요.

해결 방안:

이미 실행 중인 프로그램 종료: 현재 사용하려는 포트(예: 8080)를 이미 사용하고 있는 다른 프로그램을 찾아서 종료합니다.

윈도우 터미널(PowerShell 또는 CMD)에서 포트 확인 및 종료:

netstat -ano | findstr :8080 (8080번 포트를 사용하는 프로세스 확인)

taskkill /PID [PID_번호] /F (위에서 확인한 PID 번호로 프로세스 강제 종료)

다른 포트 번호 사용: 가장 간단한 방법이에요. 현재 사용하려는 포트 번호 대신 다른 비어있는 포트 번호(예: 8081, 5000, 9000 등)로 변경하여 프로그램을 실행합니다.

Flask 코드에서는 app.run(host='0.0.0.0', port=8080) 부분을 app.run(host='0.0.0.0', port=8081) 등으로 수정하면 됩니다.

컴퓨터 재부팅: 가끔씩 특정 프로세스가 제대로 종료되지 않아 포트가 계속 점유되어 있을 수 있어요. 이럴 땐 컴퓨터를 재부팅하면 대부분의 포트 점유 문제가 해결됩니다.