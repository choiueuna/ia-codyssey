# Git: reset과 revert의 차이점

## 1. reset (git reset)

`git reset` 명령어는 Git의 커밋 이력을 **변경(Rewriting history)**하는 강력한 도구입니다. 특정 커밋으로 돌아가면서 그 이후의 커밋들을 **없애버리거나**(Hard Reset), **스테이징 영역에서만 변경사항을 제거**하거나(Mixed Reset), **작업 디렉토리의 파일은 유지**하면서 커밋 이력만 되돌릴 수 있습니다.

* **동작 방식**: 지정된 커밋 지점으로 HEAD(현재 브랜치의 최신 커밋을 가리키는 포인터)를 이동시킵니다.
* **주요 옵션**:
    * `--hard`: 지정된 커밋 이후의 모든 변경사항(커밋 이력, 스테이징 영역, 작업 디렉토리)을 **모두 버립니다.** 가장 강력하고 위험한 옵션입니다.
    * `--mixed` (기본값): 지정된 커밋 이후의 커밋 이력은 사라지지만, 해당 변경사항들은 **스테이징되지 않은 상태**로 작업 디렉토리에 남아있습니다.
    * `--soft`: 지정된 커밋 이후의 커밋 이력은 사라지지만, 해당 변경사항들은 **스테이징된 상태**로 남아있습니다.
* **사용 예시**: 실수로 잘못 커밋했거나, 아직 외부에 공유되지 않은 개인 작업 브랜치에서 커밋을 깔끔하게 정리하고 싶을 때 사용합니다.

## 2. revert (git revert)

`git revert` 명령어는 특정 커밋에서 발생한 변경사항을 **취소하는 새로운 커밋을 생성**합니다. 이는 기존의 커밋 이력을 그대로 유지하면서, 특정 변경사항만 되돌릴 때 사용합니다.

* **동작 방식**: 되돌리고 싶은 커밋의 변경사항을 역으로 적용하는 새로운 커밋을 만들어 이력에 추가합니다.
* **주요 특징**:
    * **기존 커밋 이력을 보존합니다.** 원래 커밋이 사라지지 않고, 그 변경을 취소하는 새로운 커밋이 추가될 뿐입니다.
    * **안전합니다.** 실수로 되돌린 경우에도 취소하는 커밋을 다시 revert할 수 있습니다.
* **사용 예시**: 이미 다른 사람들과 공유된 커밋을 되돌려야 할 때 사용합니다.

## 3. 협업 시 revert를 추천하는 이유

협업 환경에서 `revert`를 `reset --hard`보다 **훨씬 강력하게 추천**하는 이유는 다음과 같습니다.

1.  **이력 보존 및 투명성**: `revert`는 기존 커밋을 삭제하지 않고 새로운 커밋을 추가하므로, **모든 변경 이력이 투명하게 보존**됩니다. 누가, 언제, 어떤 변경을 했고, 그 변경이 언제 취소되었는지 명확하게 알 수 있습니다. 이는 문제 발생 시 원인을 추적하거나 팀원 간에 상황을 공유하기 용이하게 합니다.
2.  **히스토리 왜곡 방지**: `reset --hard`는 커밋 이력을 "지워버리는" 행위입니다. 만약 이미 원격 저장소에 푸시(push)된 커밋을 `reset`으로 삭제하고 다시 푸시하려고 하면, Git은 이력 불일치(`non-fast-forward`) 오류를 발생시킵니다. 이 문제를 해결하기 위해 강제 푸시(`git push --force`)를 해야 하는데, 이는 **다른 팀원들의 Git 저장소와 불일치를 초래하고 혼란을 야기**할 수 있습니다. 다른 팀원이 이미 삭제된 커밋을 기반으로 작업하고 있다면, 큰 문제가 발생할 수 있습니다.
3.  **충돌 최소화**: `revert`는 특정 커밋의 변경사항만 역으로 적용하기 때문에, 다른 팀원이 동일한 파일에서 작업한 내용과 충돌이 발생할 가능성이 상대적으로 적습니다. `reset`은 기준점을 강제로 옮기므로, 충돌 가능성이 더 높습니다.

요약하자면, `reset`은 개인 작업 공간에서 `history`를 깔끔하게 정리할 때 유용하지만, `revert`는 **공유된 `history`를 안전하게 관리하며 협업의 혼란을 방지**하는 데 필수적인 명령어입니다.