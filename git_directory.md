버전 관리 시스템의 종류 3가지
    버전 관리 시스템(Version Control System, VCS)은 파일의 변경 이력을 추적하고 관리하여, 여러 사람이 함께 작업하거나 이전 상태로 되돌리고 싶을 때 유용하게 사용하는 도구입니다. 주요 종류는 다음과 같습니다.

    1. 로컬 VCS (Local VCS):

        가장 기본적인 형태로, 개발자 자신의 컴퓨터에 파일 변경 이력을 관리하는 방식입니다.

        모든 변경 사항이 로컬 컴퓨터에만 저장되므로, 실수로 로컬 저장소가 손상되면 모든 이력이 사라질 수 있는 단점이 있습니다.

    예시: RCS (Revision Control System)

    2. 중앙 집중식 VCS (Centralized VCS, CVCS):

        모든 개발자가 하나의 중앙 서버를 통해 파일을 관리하는 방식입니다. 개발자는 변경 사항을 서버에 '커밋(commit)'하고, 다른 개발자의 변경 사항을 서버에서 '업데이트(update)' 받습니다.

        관리가 중앙 집중적이어서 비교적 쉽고, 모든 팀원이 같은 시점의 최신 코드를 볼 수 있다는 장점이 있습니다.

        하지만 중앙 서버에 문제가 생기면 모든 개발자의 작업이 중단될 수 있고, 인터넷 연결이 필수적이라는 단점이 있습니다.

        예시: SVN (Subversion), Perforce, CVS (Concurrent Versions System)

    3. 분산형 VCS (Distributed VCS, DVCS):

        각 개발자가 중앙 서버로부터 전체 코드베이스의 '복제본(clone)'을 자신의 로컬 컴퓨터에 다운로드하여 관리하는 방식입니다. 즉, 중앙 서버뿐만 아니라 개발자 각자의 컴퓨터에도 전체 이력(히스토리)이 저장됩니다.

        중앙 서버에 문제가 생겨도 각 개발자의 로컬 저장소에 이력이 남아있어 안전하며, 오프라인에서도 작업이 가능하다는 장점이 있습니다. 팀원 간의 협업 유연성도 높습니다.

        가장 현대적이고 널리 사용되는 방식입니다.

        예시: Git, Mercurial, Darcs


# .git 디렉토리의 역할의 의미

`.git` 디렉토리는 Git 버전 관리 시스템의 **핵심이자 영혼**과 같은 존재입니다. 이 디렉토리는 Git 프로젝트의 모든 버전 관리 정보가 저장되는 숨김 폴더입니다.

`.git` 디렉토리에는 다음과 같은 주요 정보들이 포함됩니다:

1.  **객체 데이터베이스 (Object Database)**:
    * 프로젝트의 모든 파일 내용, 커밋(commit) 내역, 태그(tag), 브랜치(branch) 정보 등 Git이 관리하는 모든 데이터가 '객체(object)' 형태로 저장됩니다. Git이 파일의 변경 이력을 추적하고, 이전 버전으로 되돌리거나 병합(merge)하는 등의 모든 작업을 수행할 수 있는 근간이 됩니다.

2.  **Git 설정 파일 (Configuration Files)**:
    * 해당 Git 저장소에 특화된 설정 정보가 저장됩니다. 예를 들어, `.git/config` 파일에는 원격 저장소(remote repository) 주소, 사용자 이름 및 이메일 설정 등이 포함됩니다.

3.  **참조 (References)**:
    * 각 브랜치(branch)나 태그(tag)가 어떤 커밋을 가리키는지에 대한 정보가 저장됩니다. `.git/HEAD` 파일은 현재 작업 중인 브랜치나 커밋을 가리킵니다.

4.  **인덱스 (Index 또는 Staging Area)**:
    * 다음에 커밋할 변경 사항들을 임시로 올려두는 공간(스테이징 영역)에 대한 정보가 `.git/index` 파일에 저장됩니다.

**요약하자면, `.git` 디렉토리는 Git이 프로젝트의 모든 변경 이력을 관리하고, 버전 관리에 필요한 모든 내부적인 정보를 저장하는 중앙 저장소이자 두뇌 역할을 합니다. 이 디렉토리가 없으면 해당 폴더는 더 이상 Git 저장소로 인식되지 않으며, 모든 버전 관리 기능을 잃게 됩니다.**
